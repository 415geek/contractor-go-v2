/**
 * Contractor GO — 设计令牌 (Design Tokens)
 * 原则: 清晰层级、单一主操作、信任感、无障碍、移动优先
 *
 * 此文件从 design-tokens.ts 重新导出，保留向后兼容性
 */
export { theme, colors, spacing, borderRadius, shadows, animation, touchTarget, layout, semantic } from "./design-tokens";
export { theme as default } from "./design-tokens";
